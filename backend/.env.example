DB_NAME=vitrine_pdv
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
ALLOWED_HOSTS=localhost,127.0.0.1

# WebSocket / notificações em tempo real (channels)
# CHANNEL_LAYER=memory  # sem Redis, apenas 1 worker
# CHANNEL_LAYER=redis   # padrão; requer: docker compose up -d redis
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379

# Desenvolvimento com Expo Go no celular:
# docker compose up -d db redis
# uv run uvicorn core.asgi:application --host 0.0.0.0 --port 8000 --reload
